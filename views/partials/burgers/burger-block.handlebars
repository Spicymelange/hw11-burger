<li>
	{{name}}

	<button class="change-state" data-id="{{id}}" data-newsleep="{{devoured}}">
		{{#if devoured}}Order a new burger{{else}}Not Eaten{{/if}}
	</button>

	<button class="delete-cat" data-id="{{this.id}}">Delete</button>
</li>
